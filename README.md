# KocardPro

**KocardPro** 是一款专为影视制作现场 DIT（Digital Imaging Technician）设计的专业媒体备份软件。支持摄影机卡到多目的地的高速备份、哈希校验、PDF 报告生成，以及基于项目管理的智能化工作流。

---

## 软件功能

### 核心备份能力

- **多模式备份**：支持备卡模式（Card）、镜像模式（Mirror）、高级模式（Advanced）三种备份模式
- **并行多目的地**：同时备份至最多 4 个目的地，复制与校验并行执行
- **哈希完整性校验**：支持 MD5 / SHA-1 / SHA-256，逐文件校验，确保数据 100% 一致
- **实时进度监控**：显示传输速度、已完成文件数、ETA 剩余时间
- **任务持久化**：应用重启后自动恢复任务状态，异常退出自动标记中断任务

### 智能设备识别

- **摄影机卡自动识别**：通过多维度评分模型（协议类型、文件系统、分区方案、块大小、容量、卷名等）区分数据卡与备份硬盘
- **磁盘状态实时监控**：5 秒轮询已连接存储设备，显示容量、剩余空间、设备类型
- **设备弹出**：支持在界面内直接安全弹出外接设备

### 项目管理

- **项目模板**：预设拍摄日期范围、参与机器（A机/B机/DIT 等）、机位标签、目的地路径
- **文件目录预创建**：按拍摄计划自动批量创建 `项目名/日期/机器/机位` 层级目录结构
- **智能路径解析**：新建任务时自动关联项目，自动填充目的地路径

### 报告生成

- **PDF 备份报告**：包含任务摘要、源信息、每个目的地的文件校验列表，可导出存档

---

## 安装

### 系统要求

- macOS 11.0（Big Sur）及以上
- 支持 Apple Silicon（arm64）和 Intel（x64）

### 下载安装

前往 [Releases](https://github.com/sexyfeifan/KocardPro/releases) 页面下载最新版本：

| 架构 | 安装包 |
|------|--------|
| Apple Silicon（M1/M2/M3/M4） | `KocardPro-x.x.x-arm64.dmg` |
| Intel | `KocardPro-x.x.x-x64.dmg` |

下载 `.dmg` 后，双击打开，将 KocardPro 拖入 Applications 文件夹即可。

> **首次启动提示**：macOS 可能提示"无法打开，因为开发者未验证"。请打开「系统设置 → 隐私与安全性」，点击「仍然打开」，或运行 DMG 内附带的 `安装 KocardPro.command` 脚本自动完成授权。

---

## 使用指南

### 备卡模式（Card Mode）

将素材卡中的文件备份到指定目的地，备份目录以「卷名_时间戳」命名，确保每次备份可唯一追溯。

1. 打开软件，点击「新建备份任务」
2. 选择「备卡模式」
3. 选择素材来源目录（摄影机卡）
4. 添加目的地目录（支持多个）
5. 填写机器名称（A机/B机 等）和卷标前缀
6. 点击「开始备份」

### 镜像模式（Mirror Mode）

完整镜像素材源的目录结构与文件名，目的地内容与素材源完全一致（A = B），适用于需要制作完全相同副本的场景。

1. 选择「镜像模式」
2. 选择素材来源
3. 添加多个目的地（最多 4 个）
4. 开始备份，所有目的地并行接收数据并独立校验

### 高级模式（Advanced Mode）

关联已创建的项目，实现智能化工作流，自动识别素材卡、自动填充目的地路径，按项目层级归档。

1. 选择「高级模式」，选择对应项目
2. 软件自动识别已连接的摄影机卡作为素材源（支持手动覆盖）
3. 目的地路径自动从项目配置中填充
4. 选择拍摄日期、机器、机位，软件自动构建完整备份路径
5. 开始备份

### 查看报告

任务完成后，点击任务卡片上的「导出报告」按钮，选择保存路径，生成 PDF 备份报告。

---

## 版本历史与开发经历

### v1.5.0（2026-04）

**模式名称与说明优化**

- 将三种备份模式统一更名为更直观的名称：
  - 备卡模式（Card Mode）
  - 镜像模式（Mirror Mode）
  - 高级模式（Advanced Mode）
- 为每种模式添加独立说明横幅，包含模式特性描述与操作步骤引导，风格统一

---

### v1.4.9（2026-04）

**设备识别算法重构**

原有识别逻辑仅依赖「可移除媒体」标志和容量上限（≤512GB），对于通过 USB 连接的小容量移动硬盘会误判为数据卡，对于通过读卡器挂载的 CFexpress/XQD 卡有时无法识别。

本版本引入**多维度评分模型**，综合以下信号判断设备类型：

| 信号 | 说明 |
|------|------|
| 连接协议 | SD Card +5 / PCI-Express +3 / Thunderbolt -4 / USB -1 |
| 文件系统 | exFAT/FAT32 +2 / APFS -2 / NTFS -1 |
| 块大小 | 512B +1（卡典型值）/ 4096B -2（现代硬盘） |
| 分区方案 | FDisk +2（相机格式）/ GUID -1（Mac 格式） |
| 容量 | ≤512GB +1 / >1TB -3 |
| 可移除标志 | Removable +1 |
| 卷名规律 | A001/CARD/SONY/CANON 等 +2 |

评分 ≥ 2 → 判定为数据来源（source），否则为目的地（destination）。

---

### v1.4.8（2026-04）

**高级模式增强**

- 素材源自动识别：轮询已挂载设备，单卡自动填充，多卡显示选择器
- 备份目的地自动关联：从项目配置中读取目的地路径，实时检测设备在线状态（离线显示红色警告）
- 完整路径显示：选择来源或目的地后，在选择器下方展示完整路径便于确认

---

### v1.4.7 及以前

- 项目管理：创建/编辑/删除项目，支持设置拍摄日期范围、机器列表、机位标签
- 目录预创建：按项目配置一键批量生成拍摄日期×机器×机位的完整目录树
- PDF 报告生成：任务完成后导出含校验结果的专业备份报告
- 磁盘状态监控：Dashboard 实时显示已连接存储设备的容量与类型
- 任务持久化：重启后自动恢复历史任务

---

## 技术栈

- **框架**：Electron 28 + electron-vite
- **前端**：React 18 + TypeScript + Tailwind CSS
- **状态管理**：Zustand
- **构建**：electron-builder（生成 DMG + ZIP，支持 arm64 / x64）
- **报告**：HTML → PDF（Electron printToPDF）

---

## 开发

```bash
# 安装依赖
npm install

# 开发模式
npm run dev

# 构建
npm run build

# 打包发布（macOS arm64 + x64）
npm run dist
```

---

## 许可证

MIT License
